import ProviderGrid from "@/app/components/integrations/ProviderGrid";

export default function CalendarIntegrationPage() {
  return (
    <ProviderGrid
      category="calendar"
      title="Calendar Integrations"
      description="Connect Google Calendar, Microsoft Outlook Calendar, or Apple Calendar so Kuba can work with real business availability and scheduling."
    />
  );
}
