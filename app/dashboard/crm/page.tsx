"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
type Deal = { status: string; value: string | null };
export default function CRMPage() { const [deals, setDeals] = useState<Deal[]>([]); useEffect(() => { void fetch("/api/crm/deals", { cache: "no-store" }).then((r) => r.json()).then((d: { deals?: Deal[] }) => setDeals(d.deals || [])); }, []); const open = deals.filter((d) => d.status === "open"); return <main className="min-h-screen bg-[#050507] px-6 py-10 text-white"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[0.2em] text-cyan-300/70">Customer relationship management</p><h1 className="mt-3 text-4xl font-black">CRM Overview</h1><div className="mt-8 grid gap-4 sm:grid-cols-3"><Metric label="Open deals" value={open.length} /><Metric label="Won deals" value={deals.filter((d) => d.status === "won").length} /><Metric label="Pipeline value" value={open.reduce((sum, d) => sum + (Number(d.value) || 0), 0).toLocaleString()} /></div><Link href="/dashboard/crm/pipeline" className="mt-8 inline-block rounded-xl bg-cyan-400 px-4 py-3 text-sm font-bold text-black">Open pipeline</Link></div></main>; }
function Metric({ label, value }: { label: string; value: string | number }) { return <article className="rounded-2xl border border-white/10 bg-white/[0.03] p-5"><p className="text-xs uppercase tracking-wide text-white/40">{label}</p><p className="mt-3 text-3xl font-black">{value}</p></article>; }
