"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function NewPartnerProduct() { const router = useRouter(); const [name, setName] = useState(""); const [type, setType] = useState("AI Employee"); const [description, setDescription] = useState(""); const [error, setError] = useState(""); async function submit() { const response = await fetch("/api/partner/products", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, type, description }) }); const data = await response.json(); if (!response.ok) setError(data.error || "Unable to create draft."); else router.push("/partner"); } return <main className="min-h-screen bg-[#050507] px-6 py-10 text-white"><div className="mx-auto max-w-2xl"><Link href="/partner" className="text-sm text-white/45">← Partner portal</Link><h1 className="mt-8 text-4xl font-black">Create product draft</h1><p className="mt-3 text-sm text-white/40">Drafts require SuperKuba review before they can enter the existing Marketplace.</p><div className="mt-8 space-y-5"><Field label="Product name" value={name} setValue={setName} /><label className="block text-sm font-semibold text-white/70">Product type<select value={type} onChange={(event) => setType(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"><option>AI Employee</option><option>Skill</option><option>Automation</option><option>Package</option><option>Integration</option></select></label><label className="block text-sm font-semibold text-white/70">Description<textarea value={description} onChange={(event) => setDescription(event.target.value)} rows={5} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-4" /></label>{error && <p className="text-sm text-red-200">{error}</p>}<button type="button" onClick={() => void submit()} className="rounded-xl bg-cyan-400 px-5 py-3 text-sm font-bold text-black">Save draft</button></div></div></main>; }
function Field({ label, value, setValue }: { label: string; value: string; setValue: (value: string) => void }) { return <label className="block text-sm font-semibold text-white/70">{label}<input value={value} onChange={(event) => setValue(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3" /></label>; }
