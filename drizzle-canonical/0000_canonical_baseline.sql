-- Canonical baseline marker for the verified legacy staging schema.
-- This statement is intentionally non-destructive. Drizzle records this file's
-- hash in __drizzle_migrations after it executes successfully.
SELECT 1;
