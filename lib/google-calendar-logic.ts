export function buildGoogleEvent(appointment: { id: string; title: string; description?: string|null; startAt: Date; endAt: Date; timezone: string; location?: string|null }) { return { summary: appointment.title, description: `${appointment.description || ""}\n\nSuperKuba appointment ${appointment.id}`, location: appointment.location || undefined, start: { dateTime: appointment.startAt.toISOString(), timeZone: appointment.timezone }, end: { dateTime: appointment.endAt.toISOString(), timeZone: appointment.timezone } }; }
export function hasBusyConflict(startAt: Date, endAt: Date, busy: Array<{ start: string; end: string }>) { return busy.some((slot) => new Date(slot.start) < endAt && new Date(slot.end) > startAt); }
